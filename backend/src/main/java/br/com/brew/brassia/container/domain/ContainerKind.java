package br.com.brew.brassia.container.domain;

/** O que o contêiner é fisicamente. O tamanho não entra aqui: keg de 30 e de 50 são o mesmo bicho. */
public enum ContainerKind {
    KEG,
    CASK,
    GROWLER
}

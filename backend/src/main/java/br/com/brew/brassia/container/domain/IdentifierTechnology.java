package br.com.brew.brassia.container.domain;

/** Como o código é lido. A tecnologia não muda o que ele significa — nem o que ele não concede. */
public enum IdentifierTechnology {
    QR,
    BARCODE,
    RFID
}

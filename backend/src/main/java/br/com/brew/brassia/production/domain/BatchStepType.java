package br.com.brew.brassia.production.domain;

/** Etapas do roteiro do dia de brassa (PRD-001), derivadas da receita. */
public enum BatchStepType {
    MASH,
    BOIL,
    WHIRLPOOL,
    TRANSFER
}

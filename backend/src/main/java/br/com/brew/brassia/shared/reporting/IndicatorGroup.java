package br.com.brew.brassia.shared.reporting;

/** Os blocos do painel operacional (RPT-002). Ampliar esta lista é ampliar o painel. */
public enum IndicatorGroup {
    PRODUCTION,
    STOCK,
    QUALITY,
    FERMENTATION,
    COST
}

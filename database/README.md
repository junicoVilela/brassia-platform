# Banco de dados

Scripts são base de modelagem, não substituem migrations Flyway incrementais. Use schema `brassia`, UUID, UTC e `brewery_id`. Execute na ordem numérica em banco descartável para revisão inicial.

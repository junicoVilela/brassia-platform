{
  "name": "brassia-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Jornadas críticas de ponta a ponta (frontend + API real).",
  "scripts": {
    "serve:app": "cd ../frontend && npx ng serve --port 4200 --proxy-config ../e2e/proxy.e2e.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}

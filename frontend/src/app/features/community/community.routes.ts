import { Routes } from '@angular/router';
import { LibraryPageComponent } from './pages/library-page/library-page.component';

export const COMMUNITY_ROUTES: Routes = [{ path: '', component: LibraryPageComponent }];

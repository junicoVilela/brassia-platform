import { Routes } from '@angular/router';
import { CustomersPageComponent } from './pages/customers-page/customers-page.component';

export const CRM_ROUTES: Routes = [{ path: '', component: CustomersPageComponent }];

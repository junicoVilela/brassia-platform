import { Routes } from '@angular/router';
import { LoadsPageComponent } from './pages/loads-page/loads-page.component';

export const DISTRIBUTION_ROUTES: Routes = [{ path: '', component: LoadsPageComponent }];

import { Routes } from '@angular/router';
import { BatchesPageComponent } from './pages/batches-page/batches-page.component';

export const PRODUCTION_ROUTES: Routes = [{ path: '', component: BatchesPageComponent }];

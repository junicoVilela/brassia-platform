import { Routes } from '@angular/router';
import { AuditPageComponent } from './pages/audit-page/audit-page.component';

export const AUDIT_ROUTES: Routes = [{ path: '', component: AuditPageComponent }];

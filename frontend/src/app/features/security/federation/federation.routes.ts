import { Routes } from '@angular/router';
import { FederationPageComponent } from './pages/federation-page/federation-page.component';

export const FEDERATION_ROUTES: Routes = [{ path: '', component: FederationPageComponent }];

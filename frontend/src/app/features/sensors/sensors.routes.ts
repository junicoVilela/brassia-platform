import { Routes } from '@angular/router';
import { SensorsPageComponent } from './pages/sensors-page/sensors-page.component';

export const SENSORS_ROUTES: Routes = [{ path: '', component: SensorsPageComponent }];

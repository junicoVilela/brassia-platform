import { Routes } from '@angular/router';
import { SessionsPageComponent } from './pages/sessions-page/sessions-page.component';

export const SENSORY_ROUTES: Routes = [{ path: '', component: SessionsPageComponent }];
